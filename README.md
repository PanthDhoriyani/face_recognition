# face_recognition
A Streamlit-based web app that identifies celebrities from uploaded images using FaceNet and MTCNN. It compares facial embeddings with a stored dataset to display the closest match with a confidence score.
